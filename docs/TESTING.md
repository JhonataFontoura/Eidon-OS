# Testes

A primeira suíte usa `unittest`, da biblioteca padrão do Python.

O teste de persistência cria um banco temporário, salva uma memória e valida sua recuperação sem alterar o banco local do usuário.
